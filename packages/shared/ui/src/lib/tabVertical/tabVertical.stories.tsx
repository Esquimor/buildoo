import type { Meta } from '@storybook/react';
import { TabVertical } from './tabVertical';

const meta: Meta<typeof TabVertical> = {
  component: TabVertical,
  title: 'tabVertical',
};
export default meta;

export const Default = {
  args: {
  },
};
