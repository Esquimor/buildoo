import type { Meta } from '@storybook/react';
import { SpinnerScreen } from './spinnerScreen';

const meta: Meta<typeof SpinnerScreen> = {
  component: SpinnerScreen,
  title: 'SpinnerScreen',
};
export default meta;

export const Default = {
  args: {
  },
};
