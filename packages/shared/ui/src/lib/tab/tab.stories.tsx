import type { Meta } from '@storybook/react';
import { Tab } from './tab';

const meta: Meta<typeof Tab> = {
  component: Tab,
  title: 'tab',
};
export default meta;

export const Default = {
  args: {
  },
};
